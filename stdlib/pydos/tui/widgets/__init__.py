"""Widget collection for pydos.tui.

Import widgets from their own modules — nothing is re-exported here, so
an application only links what it actually uses:

    from pydos.tui.widgets.button import Button
    from pydos.tui.widgets.textarea import TextArea, TextDocument
"""
