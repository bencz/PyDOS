"""Color palette for the EDIT sample."""

from pydos.io.tui import Color


class EditorTheme:
    DESKTOP_FG = Color.LIGHT_GRAY
    DESKTOP_BG = Color.BLUE
    BORDER_FG = Color.LIGHT_CYAN
    CURRENT_FG = Color.BLACK
    CURRENT_BG = Color.LIGHT_CYAN
    MENU_FG = Color.BLACK
    MENU_BG = Color.LIGHT_GRAY
    MENU_SELECTED_FG = Color.WHITE
    MENU_SELECTED_BG = Color.BLUE
    STATUS_FG = Color.BLACK
    STATUS_BG = Color.LIGHT_GRAY
    SHORTCUT_FG = Color.BLACK
    SHORTCUT_BG = Color.LIGHT_CYAN
    DIALOG_FG = Color.BLACK
    DIALOG_BG = Color.LIGHT_GRAY
    INPUT_FG = Color.WHITE
    INPUT_BG = Color.BLUE
    SHADOW_FG = Color.DARK_GRAY
    SHADOW_BG = Color.BLACK
    MATCH_FG = Color.WHITE
    MATCH_BG = Color.RED
